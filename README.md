# Классификация изображений котов и собак (в нашем случае енотов) При помощи TensorFlow

Проект посвящён обучению нейросети и выполнению инференса (предсказаний) с использованием фреймворка **TensorFlow** для задачи бинарной классификации изображений: **кот 🐱 / собака 🐶(енот 🦝)**.

Проект может использоваться в учебных целях для знакомства с полным ML-циклом: от подготовки данных до применения обученной модели.

-----------------------------------------------

## 📌 Возможности проекта

- Обучение сверточной нейронной сети (CNN)
- Валидация и оценка качества модели
- Сохранение и загрузка обученной модели
- Инференс на новых изображениях
- Визуализация результатов предсказания

-----------------------------------------------

## 🧠 Используемые технологии

- Python 3.9+
- Библиотека TensorFlow / Keras
- Библиотека cv2
- Библиотека Matplotlib

-----------------------------------------------

## 📂 Пример проекта:

```text
.
import os # Импорт
import cv2 # Импорт
import tensorflow as tf # Импорт

model = tf.keras.models.load_model("Ваша модель.keras") # Переменная модели, которую вы загружаете

img = cv2.imread("test/cat11.jpg")  # Рассмотр картинки
if img is None: # Если нет каринки
    print("No image") # Уведомление если нет картинки

img = cv2.resize(img, (128, 128)) # Внесение нужного размера
img = img/255.0 # Нормализация картинки
img = tf.expand_dims(img, axis=0) # Увеличение размерности

pred = model.predict(img) # Переменная предсказывания картинки
print(pred) # Печать переменной выше
DATASET_PATH = "Ваша папка" # Переменная пути к нашей папке
class_names = os.listdir(DATASET_PATH) # Переменная со списком ваших папок
predicted_class = class_names[int(bool(pred[0]>0.5))] # Вывод предсказания
print(predicted_class) # Печать: кот или же енот (собака)
